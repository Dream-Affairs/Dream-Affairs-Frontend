name: Linter
on: push
jobs:
  ESLint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: npm
    - name: Run ESLint
      run: eslint . --ext .js,.jsx,.ts,.tsx
